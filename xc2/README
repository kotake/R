このファイルは、

makefileは、ランダムデータrandom.csvを生成し、k-meansplus.cppのコンパイル実行まで行ってくれる

output/以下には、クラスタリングによって生成されたgnuoplot用のデータファイルが格納されている

graph/以下には、クラスタリングによって生成されたxy座標のグラフデータのpng形式が格納されている。これらのグラフは、output/以下のテキストデータファイルを元に生成されている。

comb/以下にはコンビネーションを使うために、試してみたかったから作っただけでk-meansplus.cppとは直接の関係はない。

next_permutation.hppは、コンビネーションの関数を使うために必要なヘッダファイル


